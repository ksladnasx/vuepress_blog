# 原理.md —— 本项目相比「裸的 VuePress 2 默认主题博客」新增了什么、怎么加进去的、怎么实现的

> 对比基准：只用 `@vuepress/theme-default` + 最简 `config.js` 的 VuePress 2 博客（默认导航、默认侧边栏、默认首页、无评论、无博客数据）。
> 本文所有路径均以仓库根目录为起点。

---

## 一、差异总览

| 类别 | 位置 | 数量 | 作用 |
| --- | --- | --- | --- |
| 客户端配置 | `docs/.vuepress/client.js` | 1 | 注册布局、全局注册组件、注入全局副作用（KaTeX 兜底、Dify 聊天机器人、medium-zoom 容器） |
| 自定义布局 | `docs/.vuepress/layouts/` | 6 | Layout / Article / Category / Tag / Timeline / NotFound |
| 自定义组件 | `docs/.vuepress/components/` | 12 | 首页仪表盘、文章列表、可折叠侧边栏三件套、字体设置、显示设置、朗读、上下篇导航、简历经历列表等 |
| 共享数据模块 | `docs/.vuepress/backgrounds.js` | 1 | 壁纸清单 + 索引归一化/取址工具，被多个组件与构建脚本共用 |
| 全局样式 | `docs/.vuepress/styles/index.scss` | 1（约 2100 行） | CSS 变量主题系统：阅读背景、字体映射、侧边栏折叠、首页壁纸 |
| 手写 markdown-it 插件 | `docs/.vuepress/config.js` | 3 + 1 | `**标点开头加粗**`、img 路径斜杠归一化、`==高亮==`、页面稳定排序 |
| head 内联脚本 | `docs/.vuepress/config.js` | 2 | 首屏壁纸/阅读背景恢复（防 FOUC） |
| 静态后台 | `docs/.vuepress/public/admin/` | 1 | 纯静态 HTML/JS 文章管理后台（不走 Vue 构建流程） |

> 注意：`components/` 下的 `BackgroundSwitcher.vue`（独立壁纸切换按钮）和 `MermaidChart.vue`（Mermaid 渲染）**当前没有任何地方引用**，属于闲置/备用组件——壁纸功能已被 `ReadingBackgroundSwitcher` 吸收，Mermaid 渲染改用了官方 `@vuepress/plugin-markdown-chart` 插件。

---

## 二、新增组件的「挂载原理」—— 用到了 VuePress 2 的哪些扩展点

VuePress 2 不会自动扫描 `components/` 目录（那是 VuePress 1 的 register-components 插件行为），本项目所有自定义内容都是通过以下四条机制显式接进去的：

### 1. 约定式客户端配置文件 `client.js`

VuePress 2 自动将 `docs/.vuepress/client.js` 作为客户端增强入口，`defineClientConfig()` 暴露三个钩子（见 `client.js:305-332`）：

```js
export default defineClientConfig({
  layouts: { Layout, Article, Category, Tag, Timeline, NotFound },  // ① 布局注册表
  enhance({ app }) {                                                 // ② app 级增强
    app.component("HomeDashboard", HomeDashboard);        // 全局注册 → Markdown 里可直接写 <HomeDashboard />
    app.component("ResumeExperienceList", ResumeExperienceList);
  },
  setup() {                                                          // ③ 根组件级副作用
    onMounted(() => { /* KaTeX 兜底渲染、加载 Dify 聊天机器人 */ });
    router.afterEach(() => { /* 路由切换后重新跑 KaTeX、补 zoom 容器 */ });
  },
});
```

- `layouts` 把「布局名 → Vue 组件」登记进 VuePress 的布局映射表，页面按名字取用。
- `enhance` 里 `app.component()` 全局注册后，Markdown 文件（会被编译成 Vue SFC）中就能直接写组件标签。
- `setup` 在根组件作用域运行，适合挂只需要跑一次的全局逻辑。

### 2. 布局名的三条绑定途径

自定义布局如何跟某个页面对应起来：

1. **`@vuepress/plugin-blog` 的配置字段**（`config.js:611-669`）：博客插件在构建期自动生成聚合页时，`layout: "Category"` / `"Tag"` / `"Article"` / `"Timeline"` 直接指定这些页面用哪个布局。`/article/`、`/category/`、`/tag/`、`/timeline/` 四个页面就是这样产生的。
2. **404 约定**：VuePress 对未匹配路由自动渲染名为 `NotFound` 的布局，本项目提供了自定义的 `layouts/NotFound.vue`。
3. **同名兜底**：其余所有普通页面落到名为 `Layout` 的布局上，本项目用 `layouts/Layout.vue` 接管。

### 3. 「包裹默认布局 + 插槽注入」的覆盖模式

六个布局**都不是从零重写**，而是：

```vue
<script setup>
import ParentLayout from "@vuepress/theme-default/layouts/Layout.vue";  // 引入默认主题布局
</script>

<template>
  <ParentLayout>
    <template #navbar-after>  <!-- 默认主题暴露的插槽，注入自定义控件 -->
      <FontSwitcher />
      <ReadingBackgroundSwitcher />
    </template>
    <template #sidebar>       <!-- 整个侧边栏换成自研可折叠侧边栏 -->
      <CollapsibleSidebar />
    </template>
    <template #page-bottom>   <!-- 页面底部追加：朗读、上下篇、评论 -->
      <TextReader v-if="showComment" />
      <ArticleReadingLinks v-if="showComment" />
      <CommentService :darkmode="isDarkMode" />
    </template>
    <template #page>          <!-- 整页替换（列表类页面用） -->
      <ArticleList :items="filteredItems" />
    </template>
  </ParentLayout>
</template>
```

这依赖默认主题 Layout 组件预留的插槽：`#navbar-after`、`#sidebar`、`#page`、`#page-bottom`。导航栏/搜索/暗色模式等主题能力全部保留，只替换/追加需要的部分。

### 4. `@theme` 别名——复用主题内部实现

VuePress 构建时把 `@theme/*` 解析到当前激活主题包，因此自定义组件可以直接 import 默认主题的内部零件而不是重造轮子：

- `components/CollapsibleSidebar.vue:2` → `@theme/VPNavbarItems.vue`（把导航项也渲染进侧边栏）
- `components/CollapsibleSidebarItem.vue:2` → `@theme/VPAutoLink.vue`（主题的自动链接组件）
- `components/CollapsibleSidebarItems.vue:4` → `@theme/useSidebarItems`（主题的侧边栏数据源）
- `components/ReadingBackgroundSwitcher.vue:3`、`layouts/Layout.vue:4` → 默认主题的 `useDarkMode`（读写暗色模式状态）

---

## 三、布局层实现原理（6 个）

### `layouts/Layout.vue` —— 全局布局增强（核心）

- **评论区显隐**：`showComment` 计算属性判断「路径以 `/posts/` 开头且非列表页」，只有正文页才渲染 `CommentService`（Giscus，来自 `@vuepress/plugin-comment`）。
- 通过 `useDarkMode()` 把明暗状态传给评论 iframe（`:darkmode="isDarkMode"`）。
- `#navbar-after` 注入：管理后台入口（`/admin/`）、FontSwitcher、ReadingBackgroundSwitcher。
- `#sidebar` 整体替换为 CollapsibleSidebar。
- `#page-bottom` 注入 TextReader、ArticleReadingLinks 和评论区。

### `layouts/Article.vue` —— 文章列表页

`useBlogType('article')` 拿到博客插件构建期生成的「全部文章」集合，`computed` 过滤掉 `/posts/codes/`、`/posts/meaningless/` 两个目录后交给 `ArticleList`；`#page` 插槽整体替换默认页面内容。

### `layouts/Category.vue` / `layouts/Tag.vue` —— 分类页 / 标签页

结构相同：`useBlogCategory('category' | 'tag')` 返回 `{ map, currentItems, items }`。
- 顶部用 `map`（名称 → { items, path }）渲染分类/标签云，`RouteLink` 跳转到各聚合子页，`route-link-active` 类高亮当前项。
- 下方 `currentItems`（当前分类/标签下的文章）交给 ArticleList。

### `layouts/Timeline.vue` —— 时间线页

- `useBlogType('timeline')` 取数据并过滤目录；
- 自制两个下拉筛选器（年份/月份）：选项由数据动态推导，`watch(selectedYear)` 联动月份选项（切换年份后若当前月份不存在则重置为「全部」）；
- 时间轴用 CSS Grid 三列布局（卡片 3rem 中轴 卡片），奇偶项 `is-right` 交替左右，`::before` 画中轴竖线；移动端媒体查询降级为单列。

### `layouts/NotFound.vue` —— 自定义 404

- `#sidebar` 插槽塞入空占位 div，防止默认侧边栏遮挡内容；
- 显示当前丢失路径（`decodeURIComponent` 容错）、快捷入口四宫格、`/404.webp` 插图。

---

## 四、组件层实现原理（逐个拆解）

### 1. `HomeDashboard.vue` —— 首页仪表盘

- **注册方式**：`client.js` 的 `enhance` 全局注册；使用处是 `docs/README.md`（`home: true` 的首页）直接写 `<HomeDashboard />`。
- **数据来源**：`useBlogType("timeline")`、`useBlogCategory("tag")`、`useBlogCategory("category")` —— 这些是 `@vuepress/plugin-blog` 提供的客户端组合式函数，数据在**构建期**由博客插件从所有页面的 frontmatter 抽取生成，运行时零请求。
- **逻辑**：`computed` 过滤掉三个目录 → 取前 4 篇作「最近在写」；统计卡（公开笔记/分类/标签/时间线数量）也全部由这三个响应式数据派生；点击卡片 `router.push`。
- 摘要展示前用 `cleanExcerpt` 把 HTML 标签剥掉。

### 2. `ArticleList.vue` —— 文章卡片列表（复用组件）

- **props 驱动**：`items`（博客插件的标准结构 `[{ info, path }]`）、`isTimeline`、`kind`。被 Article / Category / Tag 三个布局复用。
- 摘要区 `v-html` 渲染（excerpt 由博客插件在构建期截取）；`-webkit-line-clamp` 控制三行截断。
- 整卡点击导航 `$router.push(path)`，样式大量使用主题 CSS 变量（`--vp-c-*`）保证明暗主题自适应；SCSS 里 `@use '@vuepress/theme-default/styles/mixins'` 复用主题的 `content-wrapper` mixin 对齐正文宽度。

### 3. `CollapsibleSidebar.vue` + `CollapsibleSidebarItems.vue` + `CollapsibleSidebarItem.vue` —— 可折叠侧边栏三件套

- **替换点**：`Layout.vue` 的 `#sidebar` 插槽，替换默认主题的 VPSidebar。
- **折叠原理（属性选择器方案）**：组件不在自己身上写折叠样式，而是对 `document.documentElement` toggle `data-sidebar-collapsed` 属性（`toggleAttribute`），真正的布局变化写在全局 `index.scss:1287-1332` 的 `html[data-sidebar-collapsed] .vp-page { ... }` 等选择器里——这样**跨组件层级**（侧边栏、正文、分页导航、评论区都要跟着变宽）的布局调整不需要往组件深处传状态。状态持久化到 `localStorage`（key：`xh-sidebar-collapsed`）。
- **侧边栏内容**：复用主题 `VPNavbarItems`（导航项） + `CollapsibleSidebarItems`（文档侧边栏项）。后者用 `@theme/useSidebarItems` 拿到与默认主题完全相同的侧边栏数据，并监听路由自动把 active 项 `scrollIntoView`。
- **Item 是递归组件**：`CollapsibleSidebarItem` 在自己的模板里继续渲染 `CollapsibleSidebarItem` 处理 children，`depth` prop 控制缩进/字号。自己实现了 active 判断（路径去掉 `#hash`、`index.md/html` 后缀再比对，哈希也参与比较）、默认展开规则（depth 0 或自身 active 时展开）、路由切换时自动展开 active 项、`<Transition>` 折叠动画。
- 箭头展开按钮与标题链接分离，互不干扰。

### 4. `FontSwitcher.vue` —— 字体设置面板（navbar「Aa」按钮）

- **生效原理：不操作任何具体元素，只改 `<html>` 的 dataset 和 CSS 变量**：
  - `root.dataset.font = 'lxgw'` → 命中全局样式 `html[data-font="lxgw"]`（`index.scss:311` 等）切换 `--xh-font-family`；
  - `root.style.setProperty("--xh-font-size", "20px")` 等写入字号/行高/字距/字重，全站样式都基于这些变量计算（如 `calc(var(--xh-font-size) * 0.82)`）；
  - 自定义正文颜色时直接覆写主题变量 `--vp-c-text` 系列。
- **字体懒加载**：5 种网络字体用 `FontFace` API 动态加载（`src` 先 `local()` 找本机字体，miss 才下载 `/xxx.woff2`），`requestIdleCallback` 空闲调度避免阻塞；加载失败降级为往 `<head>` 注入 `@font-face` 样式标签；同一字体用 `Set`/`Map` 去重防重复加载。
- **多面板互斥**：与 ReadingBackgroundSwitcher 通过 `document` 自定义事件 `xh-settings-panel-open` 通信，一方打开即通知另一方关闭；Esc/点击外部关闭。

### 5. `ReadingBackgroundSwitcher.vue` —— 显示设置面板（navbar「显」按钮）

功能最重的组件，三块能力：

- **阅读背景（仅正文页显示）**：10 种背景（纸页/青雾/…/纯黑），核心动作只有一行 `document.documentElement.dataset.readingBg = key` —— 每种背景的完整配色在 `index.scss:156-308` 以 `html[data-reading-bg="paper"] { --xh-reading-content-bg: ... }` 形式定义，组件只负责切 dataset。背景自带明暗归属（scheme: light/dark），选择时联动 `useDarkMode` 切主题并 toast 提示；主题切换后若当前背景不兼容则自动回退默认（`watch(isDarkMode)`）。
- **页面亮度**：不使用 `filter: brightness()`（会破坏 `position: fixed` 元素的层叠上下文），而是写 `--xh-page-brightness-overlay-color/opacity` 两个变量，由 `index.scss:526-532` 的 `body::after` 全屏覆盖层实现提亮/压暗。
- **首页壁纸**：「固定/随机」两种模式。固定模式轮换 `backgrounds.js` 的清单并写 `--xh-home-bg-desktop[-dark]` / `--xh-home-bg-mobile[-dark]`；随机模式从壁纸 API 生成随机 URL，先用 `new Image()` **预加载成功后才应用**（避免闪黑屏），失败回退固定壁纸并 toast。桌面/移动由 `matchMedia("(max-width: 719px)")` 决定，断点变化自动切换目标。
- **防首屏闪烁（FOUC）配套**：Vue 应用挂载前组件还不存在，因此 `config.js` 的 `head` 里注入了两段内联脚本（`restoreBackgroundScript`、`restoreReadingBackgroundScript`，由 `backgrounds.js` 序列化生成），在 HTML 解析阶段就从 localStorage 恢复壁纸变量和 `data-reading-bg`，与组件的持久化格式严格对齐。

### 6. `TextReader.vue` —— 文本朗读悬浮球

基于浏览器原生 **Web Speech API**（`speechSynthesis`），零后端：

- **内容抓取**：`querySelector(".vp-page [vp-content]")` 取正文 → `cloneNode` 后移除 `pre/script/style/按钮/标题锚点` 等噪音 → 纯文本。
- **分块**：按中英文标点正则切句，累积到 180 字为一块（TTS 单段过长易中断，分块逐段播放）。
- **起点定位**：优先 `route.hash` 匹配标题，否则按滚动位置（`getBoundingClientRect().top <= 128`）推断当前所在小节；用 `Range` API 提取「该标题 → 文末」的内容，实现「从当前位置读起」。
- **播放队列**：`speakChunk(index)` 创建 `SpeechSynthesisUtterance`，`onend` 回调递归播下一块；用自增 `speechRunId` 给每轮播放打标，过期回调（停止/切换后仍到达的 `onend`）直接忽略，防止状态错乱。语速/音调/音量/音色（`getVoices()` 去重排序、中文优先）均实时映射到 utterance；设置变更时以「暂停 + 标记 resume 时重播当前块」的方式生效。
- **拖拽悬浮球**：Pointer Events（`pointerdown/move/up` + `setPointerCapture`）；位移 >4px 判定拖拽（保存位置到 localStorage），否则判定点击（展开面板）；面板弹出前检测四周空间，动态决定上/下、左/右展开（`panelPlacement`）。路由切换自动停止朗读并重取文本。

### 7. `ArticleReadingLinks.vue` —— 同分类上一篇/下一篇

- `useBlogType("article")` 取全部文章 → 用当前页路径（`decodeURI` 容错后比对）定位当前文章 → 过滤出**同分类**且有发布时间的文章 → 按发布时间排序取「大于当前时间的最近一篇」为上一篇、「小于当前时间的最近一篇」为下一篇。全部 `computed` 派生，无额外构建配置。
- 渲染在 `Layout.vue` 的 `#page-bottom`，仅在正文页显示。

### 8. `ResumeExperienceList.vue` —— 简历经历列表

最简单的 props 组件：接收 `items: [{ time, title, description }]` 渲染成卡片时间线。全局注册后在 `docs/get-started.md` 的 `<script setup>` 中定义数据、模板里 `<ResumeExperienceList :items="experiences" />` 使用——展示了「Markdown 内嵌 `<script setup>` 传参」的用法。

### 9. `MermaidChart.vue`（当前闲置）

接收 `code` prop，`await import("mermaid")` **动态 import 按需加载** Mermaid（不进首屏 bundle），`mermaid.render()` 产出 SVG 后 `v-html` 插入，`watch(code)` 重渲染，失败时输出错误占位。站点目前的 Mermaid 渲染实际由官方 `markdownChartPlugin({ mermaid: true })` 在 Markdown 层完成，此组件为备用实现。

### 10. `BackgroundSwitcher.vue`（当前闲置）

独立壁纸切换按钮，实现要点已被 ReadingBackgroundSwitcher 吸收，但保留了两个值得注意的技术点：基于 `<link rel="prefetch" as="image" fetchpriority="low">` 的**低优先级壁纸预取队列**（`requestIdleCallback` + 700ms 步进节流），以及用 **Network Information API**（`connection.saveData`、`effectiveType`）在省流量模式/2G 下跳过预取。

---

## 五、与组件配套的其他自定义（config.js / client.js 层）

这些不是组件，但支撑了组件体系的运转：

1. **三个手写 markdown-it 插件**（`config.js`）：
   - `markdownPunctuationStrongPlugin`：`md.inline.ruler.before("emphasis")` 注入规则，让 `**「标点开头的文字」**` 也能加粗（默认 emphasis 规则对标点开头的闭合判定有缺陷）；
   - `markdownImagePathPlugin`：`md.core.ruler.before("normalize")` 把 HTML `<img src>` 里的反斜杠统一成正斜杠（Windows 下的图片路径容错）；
   - `markdownHighlightPlugin`：`md.core.ruler.after("inline")` 用保守正则把 `==文字==` 替换成 `mark_open/mark_close` token（避免与 `===`、代码块冲突）。
   - 编写原理：markdown-it 是 token 流管线，插件即在指定阶段前/后插入 rule 函数，直接操纵 token（或 `state.src`）。

2. **`stablePageOrderPlugin`**：VuePress 插件对象 `{ name, onInitialized(app) { app.pages.sort(...) } }`，在博客插件**前后各包一次**对 `app.pages` 做确定性排序（sticky → 日期 → 路径），消除构建产物顺序抖动。

3. **client.js 中的 Dify 聊天机器人集成**：动态 `<script>` 注入 `udify.app/embed.min.js`；`MutationObserver` 监听 body 等待第三方按钮挂载；Pointer Events 实现悬浮球拖拽（位置存 localStorage），拖动 >4px 抑制随后的 click；聊天窗位置用 `requestAnimationFrame` 跟随按钮重算，`clamp` 保证不出屏。

4. **KaTeX 双保险**：构建期 `markdown-it-katex` 渲染公式；`client.js` 里又引入 CDN 的 auto-render，对 `.vp-page [vp-content]` 再跑一次 `renderMathInElement`，兜住运行时生成的动态内容。

5. **Vite 层配置**（`config.js` bundler）：`@vueuse/core` 别名指到源码入口解决多版本去重；dev server 代理 `/github-api`、`/github-assets` 减轻本地 CORS（Giscus/后台用）。

---

## 六、整体数据流小结

```
构建期（Node）                                 运行期（浏览器）
─────────────────────                        ─────────────────────────
md frontmatter                                plugin-blog 的 client API
   │                                             │
   ▼                                             ▼
@vuepress/plugin-blog 抽取       useBlogType / useBlogCategory（响应式）
category / tag / article /                    │
timeline 四类聚合页 + 数据        ┌────────────┼─────────────────┐
   │                              ▼            ▼                 ▼
   │                        HomeDashboard  ArticleList   ArticleReadingLinks
   ▼                              （由 layouts / README.md 挂载）
布局名映射（client.js layouts）
   │
   ├─ /            → Layout(默认主题, home) + <HomeDashboard/>
   ├─ /article/    → Article  → ArticleList
   ├─ /category/   → Category → 分类云 + ArticleList
   ├─ /tag/        → Tag      → 标签云 + ArticleList
   ├─ /timeline/   → Timeline → 年月筛选时间轴
   ├─ /posts/**    → Layout（+ TextReader + 上下篇 + 评论）
   └─ 404          → NotFound

用户偏好状态（localStorage）：
  字体/字号/字重…  → html dataset + CSS 变量 → index.scss 命中
  阅读背景        → html[data-reading-bg]  → index.scss 配色
  首页壁纸        → --xh-home-bg-* 变量     → 首页背景（head 内联脚本防 FOUC）
  侧边栏折叠      → html[data-sidebar-collapsed] → index.scss 布局
```

**一句话总结**：本项目在默认主题之上，以 `client.js`（layouts 注册 + 全局组件注册 + 全局副作用）为入口、以「包裹默认布局 + 主题插槽注入」为覆盖模式、以 `@theme` 别名复用主题内部件、以「写 `<html>` 的 dataset/CSS 变量 + 全局 SCSS 选择器」为跨组件样式状态方案，搭出了一套带博客数据（plugin-blog）、可深度定制的阅读体验层。
